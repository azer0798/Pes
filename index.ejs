<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; --text: #111827; --border: #e5e7eb; }
        body { background: #ffffff; color: var(--text); font-family: 'Segoe UI', sans-serif; margin: 0; padding-top: 40px; }

        .announcement-bar {
            background: #111827; color: #ffffff; height: 40px; line-height: 40px; 
            font-size: 13px; position: fixed; top: 0; width: 100%; z-index: 2000; overflow: hidden;
        }
        .ticker { display: inline-block; animation: ticker-ltr 25s linear infinite; white-space: nowrap; }
        @keyframes ticker-ltr { 0% { transform: translateX(-100%); } 100% { transform: translateX(100%); } }

        .navbar { border-bottom: 1px solid var(--border); background: #fff; position: sticky; top: 40px; z-index: 1000; padding: 15px 0; }
        .navbar-brand { font-weight: 900; font-size: 1.8rem; color: var(--primary) !important; margin: 0 auto; text-transform: uppercase; }

        .service-nav { display: flex; justify-content: center; gap: 10px; margin: 25px 0; flex-wrap: wrap; }
        .btn-service { border: 1px solid var(--border); color: var(--text); padding: 8px 18px; border-radius: 50px; text-decoration: none; font-size: 14px; font-weight: 600; transition: 0.2s; }
        .btn-service:hover { border-color: var(--primary); color: var(--primary); }

        .filter-bar { display: flex; justify-content: space-between; align-items: center; padding: 10px 0; margin-bottom: 20px; border-bottom: 1px solid var(--border); }
        .sort-btn { background: none; border: none; color: #64748b; font-size: 14px; font-weight: 600; cursor: pointer; margin-right: 15px; }
        
        .account-card { border: 1px solid var(--border); border-radius: 15px; overflow: hidden; transition: 0.3s; background: #fff; }
        .carousel-item img { height: 210px; object-fit: cover; }
        .carousel-control-prev, .carousel-control-next { width: 10%; filter: drop-shadow(0px 0px 2px rgba(0,0,0,0.5)); }

        .price-dz { font-size: 1.4rem; font-weight: 800; color: #16a34a; display: block; }
        .price-us { font-size: 0.9rem; color: #64748b; font-weight: 600; }
        
        .btn-buy { background: var(--primary); color: #fff; border: none; width: 100%; padding: 12px; border-radius: 10px; font-weight: 700; text-decoration: none; display: block; text-align: center; }

        footer { padding: 50px 0; border-top: 1px solid var(--border); text-align: center; margin-top: 60px; background: #fafafa; }
        .footer-logo { font-size: 1.5rem; font-weight: 900; color: var(--text); margin-bottom: 10px; }
    </style>
</head>
<body>

<div class="announcement-bar text-center">
    <div class="ticker"><%= settings.announcement %> &nbsp;&nbsp;&nbsp; | &nbsp;&nbsp;&nbsp; WassitDZ Game ⚡</div>
</div>

<nav class="navbar">
    <div class="container d-flex"><a class="navbar-brand" href="/">WASSITDZ GAME</a></div>
</nav>

<div class="container mt-2">
    <div class="service-nav">
        <a href="<%= settings.supportLink %>" class="btn-service">الدعم الفني</a>
        <a href="<%= settings.mediationLink %>" class="btn-service">الوساطة</a>
        <a href="<%= settings.sellAccountLink %>" class="btn-service">بيع حسابك لنا</a>
    </div>

    <div class="filter-bar">
        <span class="fw-bold small">الحسابات المعروضة</span>
        <div>
            <button class="sort-btn" onclick="sortItems(1)">الأقل سعراً</button>
            <button class="sort-btn" onclick="sortItems(-1)">الأعلى سعراً</button>
        </div>
    </div>

    <div class="row g-4" id="accountsGrid">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4 col-sm-6 account-item" data-price="<%= acc.priceDZ %>">
            <div class="account-card">
                <div id="c-<%= acc.id %>" class="carousel slide" data-bs-ride="false" data-bs-interval="false">
                    <div class="carousel-inner">
                        <% acc.imgs.forEach((img, i) => { %>
                        <div class="carousel-item <%= i==0?'active':'' %>"><img src="<%= img %>" class="d-block w-100"></div>
                        <% }) %>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#c-<%= acc.id %>" data-bs-slide="prev"><span class="carousel-control-prev-icon"></span></button>
                    <button class="carousel-control-next" type="button" data-bs-target="#c-<%= acc.id %>" data-bs-slide="next"><span class="carousel-control-next-icon"></span></button>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between mb-2 small">
                        <span class="text-muted fw-bold">ID: #<%= acc.id %></span>
                        <span class="badge bg-light text-dark border"><%= acc.linkType %></span>
                    </div>
                    <h5 class="fw-bold mb-2"><%= acc.title %></h5>
                    <div class="mb-4">
                        <span class="price-dz"><%= acc.priceDZ %> د.ج</span>
                        <span class="price-us">$<%= acc.priceUSD %></span>
                    </div>
                    <a href="<%= settings.buyNowLink %>?text=<%= encodeURIComponent('طلب شراء حساب رقم: #' + acc.id) %>" class="btn btn-buy">شراء الحساب الآن</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<footer>
    <div class="footer-logo"><i class="fas fa-gamepad me-2"></i> WASSITDZ GAME</div>
    <p class="text-muted small">جميع الحقوق محفوظة © 2026</p>
</footer>

<script>
    function sortItems(dir) {
        const grid = document.getElementById('accountsGrid');
        const items = Array.from(grid.getElementsByClassName('account-item'));
        items.sort((a, b) => (parseFloat(a.dataset.price) - parseFloat(b.dataset.price)) * dir);
        items.forEach(it => grid.appendChild(it));
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
