<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | متجر الحسابات</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #0f172a; color: #f8fafc; font-family: sans-serif; }
        .navbar { background-color: #1e293b; border-bottom: 1px solid #334155; }
        .hero { background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%); padding: 30px 0; border-bottom: 1px solid #334155; }
        .card-pes { background: #1e293b; border: 1px solid #334155; border-radius: 15px; transition: 0.3s; position: relative; }
        .card-pes:hover { transform: translateY(-5px); border-color: #3b82f6; }
        .price { font-size: 1.4rem; color: #22c55e; font-weight: 800; }
        .btn-buy { background-color: #0088cc; border: none; font-weight: bold; }
        .account-id-badge { background: rgba(59, 130, 246, 0.2); color: #3b82f6; padding: 2px 8px; border-radius: 5px; font-size: 0.8rem; margin-bottom: 10px; display: inline-block; }
        .featured-badge { position: absolute; top: 15px; right: 15px; background: #eab308; color: #000; padding: 2px 10px; border-radius: 5px; font-weight: bold; font-size: 0.8rem; z-index: 10; }
    </style>
</head>
<body>

<nav class="navbar navbar-dark">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/"><i class="fas fa-gamepad text-info"></i> WassitDZ Game</a>
    </div>
</nav>

<header class="hero text-center">
    <div class="container">
        <h1 class="fw-bold text-white">WassitDZ Game</h1>
        <p class="text-secondary small">متجر بيع حسابات eFootball الموثوق في الجزائر</p>
    </div>
</header>

<div class="container my-5">
    <div class="row g-4">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4">
            <div class="card card-pes h-100">
                <% if(acc.featured) { %> <span class="featured-badge">عرض خاص</span> <% } %>
                <img src="<%= acc.img %>" class="card-img-top p-2 rounded-4" style="height: 180px; object-fit: cover;">
                <div class="card-body">
                    <span class="account-id-badge">كود الحساب: #<%= acc.id %></span>
                    <h5 class="card-title fw-bold text-white"><%= acc.title %></h5>
                    <p class="text-secondary small mb-3"><%= acc.players %></p>
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="price">$<%= acc.price %></span>
                        <a href="https://t.me/zedx07?text=السلام عليكم، أريد شراء الحساب رقم: #<%= acc.id %>" class="btn btn-buy text-white px-4">
                            شراء الآن
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<footer class="text-center py-4 text-secondary border-top border-secondary">
    <p>© 2026 WassitDZ Game - جميع الحقوق محفوظة</p>
</footer>

</body>
</html>
