<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | متجر الحسابات</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: #3b82f6; --dark-bg: #0f172a; --card-bg: #1e293b; --accent: #22c55e; }
        body { background-color: var(--dark-bg); color: #f1f5f9; font-family: 'Segoe UI', sans-serif; overflow-x: hidden; }
        
        /* الشريط المتحرك */
        .news-ticker { background: linear-gradient(90deg, #2563eb, #3b82f6); color: white; padding: 10px 0; overflow: hidden; white-space: nowrap; font-weight: bold; border-bottom: 2px solid rgba(255,255,255,0.1); }
        .ticker-content { display: inline-block; animation: ticker-rtl 30s linear infinite; padding-right: 50px; }
        @keyframes ticker-rtl { 0% { transform: translateX(100%); } 100% { transform: translateX(-100%); } }

        .hero { padding: 40px 0; border-bottom: 1px solid #334155; margin-bottom: 20px; }
        .logo-text { font-size: 2.8rem; font-weight: 900; color: #fff; text-shadow: 0 0 15px rgba(59,130,246,0.5); letter-spacing: -1px; }
        
        .service-bar { display: flex; gap: 12px; justify-content: center; margin-bottom: 40px; flex-wrap: wrap; }
        .btn-service { background: var(--card-bg); border: 1px solid #334155; color: #fff; padding: 12px 22px; border-radius: 15px; transition: 0.3s; text-decoration: none; font-weight: 600; display: flex; align-items: center; gap: 8px; font-size: 0.95rem; }
        .btn-service:hover { background: var(--primary); transform: translateY(-3px); border-color: var(--primary); color: white; }

        .card-pes { background: var(--card-bg); border: 1px solid #334155; border-radius: 20px; overflow: hidden; transition: 0.4s; height: 100%; position: relative; }
        .card-pes:hover { border-color: var(--primary); box-shadow: 0 15px 40px rgba(0,0,0,0.6); }
        .carousel-item img { height: 220px; object-fit: cover; }
        
        .badge-link { background: #475569; color: #e2e8f0; font-size: 0.75rem; padding: 4px 10px; border-radius: 8px; }
        .badge-id { color: var(--primary); font-weight: bold; font-size: 0.85rem; }
        .price-tag { font-size: 1.6rem; color: var(--accent); font-weight: 800; }
        .btn-buy { background: var(--primary); border: none; padding: 12px; border-radius: 12px; font-weight: 700; width: 100%; color: white; text-decoration: none; display: block; text-align: center; transition: 0.3s; }
        .btn-buy:hover { background: #2563eb; transform: scale(1.03); color: white; }
        
        .featured-tag { position: absolute; top: 12px; right: 12px; background: #f59e0b; color: #000; padding: 4px 12px; border-radius: 8px; font-weight: 900; z-index: 10; font-size: 0.8rem; }
    </style>
</head>
<body>

<div class="news-ticker">
    <div class="ticker-content">
        <i class="fas fa-bullhorn"></i> &nbsp; <%= settings.announcement %> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp; | &nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <i class="fas fa-shield-alt"></i> وسيطكم المعتمد WassitDZ دائماً في الخدمة
    </div>
</div>

<header class="hero text-center">
    <div class="container">
        <h1 class="logo-text">WASSITDZ GAME</h1>
        <p class="text-secondary">الخيار الأول والمضمون لتداول حسابات eFootball</p>
    </div>
</header>

<div class="container">
    <div class="service-bar">
        <a href="<%= settings.supportLink %>" class="btn-service"><i class="fas fa-headset text-info"></i> الدعم الفني</a>
        <a href="<%= settings.mediationLink %>" class="btn-service"><i class="fas fa-shield-alt text-warning"></i> خدمة الوساطة</a>
        <a href="<%= settings.sellAccountLink %>" class="btn-service"><i class="fas fa-hand-holding-usd text-success"></i> بيع حسابك لنا</a>
    </div>

    <div class="row g-4 mb-5">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4">
            <div class="card card-pes">
                <% if(acc.featured) { %> <div class="featured-tag">مميز ✨</div> <% } %>

                <div id="slider-<%= acc.id %>" class="carousel slide">
                    <div class="carousel-inner">
                        <% acc.imgs.forEach((img, index) => { %>
                            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                <img src="<%= img %>" class="d-block w-100">
                            </div>
                        <% }) %>
                    </div>
                    <% if(acc.imgs.length > 1) { %>
                    <button class="carousel-control-prev" type="button" data-bs-target="#slider-<%= acc.id %>" data-bs-slide="prev"><span class="carousel-control-prev-icon"></span></button>
                    <button class="carousel-control-next" type="button" data-bs-target="#slider-<%= acc.id %>" data-bs-slide="next"><span class="carousel-control-next-icon"></span></button>
                    <% } %>
                </div>

                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <span class="badge-id">ID: #<%= acc.id %></span>
                        <span class="badge-link"><i class="fas fa-link"></i> <%= acc.linkType %></span>
                    </div>
                    <h5 class="fw-bold mb-1 text-white"><%= acc.title %></h5>
                    <p class="text-secondary small mb-4"><%= acc.players %></p>
                    <div class="d-flex justify-content-between align-items-center mt-auto">
                        <span class="price-tag">$<%= acc.price %></span>
                        <div style="width: 50%;">
                            <a href="https://t.me/zedx07?text=<%= encodeURIComponent('السلام عليكم WassitDZ، أريد شراء الحساب رقم: #' + acc.id) %>" class="btn btn-buy">شراء</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
