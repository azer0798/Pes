<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | متجر eFootball</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #0b0f1a; color: #f8fafc; font-family: 'Segoe UI', sans-serif; }
        .hero { background: linear-gradient(135deg, #1e293b 0%, #0b0f1a 100%); padding: 30px 0; border-bottom: 2px solid #3b82f6; }
        .card-pes { background: #161e2e; border: 1px solid #2d3748; border-radius: 15px; overflow: hidden; transition: 0.3s; }
        .card-pes:hover { transform: translateY(-5px); border-color: #3b82f6; }
        .price { font-size: 1.5rem; color: #10b981; font-weight: 800; }
        .btn-buy { background-color: #0088cc; border: none; font-weight: bold; border-radius: 10px; width: 100%; }
        .acc-id { color: #3b82f6; background: rgba(59, 130, 246, 0.1); padding: 2px 8px; border-radius: 5px; font-size: 0.8rem; font-weight: bold; }
        .carousel-item img { height: 220px; object-fit: cover; }
        .featured-tag { position: absolute; top: 10px; right: 10px; background: #fbbf24; color: #000; padding: 2px 10px; border-radius: 5px; font-weight: bold; font-size: 0.75rem; z-index: 10; }
    </style>
</head>
<body>

<header class="hero text-center mb-4">
    <div class="container">
        <h1 class="fw-bold"><i class="fas fa-gamepad text-info"></i> WassitDZ Game</h1>
        <p class="text-secondary small">رقم #1 في الجزائر لبيع حسابات eFootball</p>
    </div>
</header>

<div class="container mb-5">
    <div class="row g-4">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4">
            <div class="card card-pes h-100">
                <% if(acc.featured) { %> <span class="featured-tag">🔥 عرض ممتاز</span> <% } %>
                
                <div id="slider-<%= acc.id %>" class="carousel slide" data-bs-ride="false">
                    <div class="carousel-inner">
                        <% acc.imgs.forEach((img, index) => { %>
                            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                <img src="<%= img %>" class="d-block w-100">
                            </div>
                        <% }) %>
                    </div>
                    <% if(acc.imgs.length > 1) { %>
                    <button class="carousel-control-prev" type="button" data-bs-target="#slider-<%= acc.id %>" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#slider-<%= acc.id %>" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    </button>
                    <% } %>
                </div>

                <div class="card-body d-flex flex-column">
                    <div class="mb-2"><span class="acc-id">ID: #<%= acc.id %></span></div>
                    <h5 class="card-title fw-bold text-white"><%= acc.title %></h5>
                    <p class="text-secondary small flex-grow-1"><%= acc.players %></p>
                    <div class="mt-3">
                        <div class="text-center mb-2"><span class="price">$<%= acc.price %></span></div>
                        <a href="https://t.me/zedx07?text=<%= encodeURIComponent('السلام عليكم WassitDZ Game، أريد شراء الحساب رقم: #' + acc.id) %>" 
                           class="btn btn-buy text-white py-2">
                            شراء الآن
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
