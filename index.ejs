<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | المتجر الرسمي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; --text: #111827; --border: #e5e7eb; }
        body { background: #ffffff; color: var(--text); font-family: 'Segoe UI', sans-serif; margin: 0; padding-top: 40px; }

        /* الشريط الإشهاري الثابت */
        .announcement-bar {
            background: #111827; color: #ffffff; height: 40px; line-height: 40px; 
            font-size: 13px; position: fixed; top: 0; width: 100%; z-index: 2000; overflow: hidden;
        }
        .ticker { display: inline-block; animation: ticker-ltr 25s linear infinite; white-space: nowrap; }
        @keyframes ticker-ltr { 0% { transform: translateX(-100%); } 100% { transform: translateX(100%); } }

        /* الهيدر الموسط */
        .navbar { 
            border-bottom: 1px solid var(--border); background: rgba(255,255,255,0.9); 
            backdrop-filter: blur(10px); position: sticky; top: 40px; z-index: 1000; padding: 15px 0;
        }
        .navbar-brand { font-weight: 900; font-size: 1.8rem; color: var(--primary) !important; margin: 0 auto; text-transform: uppercase; }

        .service-nav { display: flex; justify-content: center; gap: 10px; margin: 25px 0; flex-wrap: wrap; }
        .btn-service { 
            border: 1px solid var(--border); color: var(--text); padding: 8px 18px; 
            border-radius: 50px; text-decoration: none; font-size: 14px; transition: 0.2s; font-weight: 600;
        }
        .btn-service:hover { border-color: var(--primary); color: var(--primary); background: #f9fafb; }

        /* كرت الحساب */
        .account-card { border: 1px solid var(--border); border-radius: 15px; overflow: hidden; transition: 0.3s; background: #fff; height: 100%; }
        .account-card:hover { border-color: var(--primary); transform: translateY(-4px); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .carousel-item img { height: 210px; object-fit: cover; }
        
        .price-dz { font-size: 1.4rem; font-weight: 800; color: #16a34a; display: block; margin-bottom: 2px; }
        .price-us { font-size: 0.9rem; color: #64748b; font-weight: 600; }
        
        .btn-buy { 
            background: var(--primary); color: #fff; border: none; width: 100%; 
            padding: 12px; border-radius: 10px; font-weight: 700; text-decoration: none; display: block; text-align: center; transition: 0.2s;
        }
        .btn-buy:hover { opacity: 0.9; color: #fff; }

        /* التذييل */
        footer { padding: 50px 0; border-top: 1px solid var(--border); text-align: center; margin-top: 60px; background: #fafafa; }
        .footer-logo { font-size: 1.5rem; font-weight: 900; color: var(--text); margin-bottom: 10px; }
    </style>
</head>
<body>

<div class="announcement-bar text-center">
    <div class="ticker"><i class="fas fa-bullhorn me-2"></i> <%= settings.announcement %> &nbsp;&nbsp;&nbsp; | &nbsp;&nbsp;&nbsp; WassitDZ Game ⚡</div>
</div>

<nav class="navbar">
    <div class="container d-flex"><a class="navbar-brand" href="/">WASSITDZ GAME</a></div>
</nav>

<div class="container mt-2">
    <div class="service-nav">
        <a href="<%= settings.supportLink %>" class="btn-service">الدعم الفني</a>
        <a href="<%= settings.mediationLink %>" class="btn-service">الوساطة المضمونة</a>
        <a href="<%= settings.sellAccountLink %>" class="btn-service">بيع حسابك لنا</a>
    </div>

    <div class="row g-4">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4 col-sm-6">
            <div class="account-card">
                <div id="c-<%= acc.id %>" class="carousel slide" data-bs-ride="false">
                    <div class="carousel-inner">
                        <% acc.imgs.forEach((img, i) => { %>
                        <div class="carousel-item <%= i==0?'active':'' %>"><img src="<%= img %>" class="d-block w-100"></div>
                        <% }) %>
                    </div>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between mb-2 small">
                        <span class="text-muted fw-bold">ID: #<%= acc.id %></span>
                        <span class="badge bg-light text-dark border"><%= acc.linkType %></span>
                    </div>
                    <h5 class="fw-bold mb-2"><%= acc.title %></h5>
                    <p class="small text-muted mb-4 text-truncate"><%= acc.players %></p>
                    
                    <div class="mb-4">
                        <span class="price-dz"><%= acc.priceDZ %> د.ج</span>
                        <span class="price-us">$<%= acc.priceUSD %> (Binance/USDT)</span>
                    </div>
                    <a href="https://t.me/zedx07?text=<%= encodeURIComponent('طلب شراء حساب رقم: #' + acc.id) %>" class="btn btn-buy">شراء الحساب الآن</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<footer>
    <div class="container">
        <div class="footer-logo"><i class="fas fa-gamepad me-2"></i> WASSITDZ GAME</div>
        <p class="text-muted mb-1">جميع الحقوق محفوظة © 2026</p>
        <div class="small text-muted">منصتكم الموثوقة لتداول الحسابات في الجزائر</div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
