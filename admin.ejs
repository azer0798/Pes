<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم | إضافة حسابات</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>
        body { background-color: #f1f5f9; }
        .admin-header { background: #1e293b; color: white; padding: 20px 0; margin-bottom: 30px; }
    </style>
</head>
<body>

<div class="admin-header text-center">
    <h2>إدارة متجر PES</h2>
    <a href="/" class="btn btn-outline-light btn-sm mt-2">معاينة المتجر</a>
    <a href="/logout" class="btn btn-danger btn-sm mt-2">خروج</a>
</div>

<div class="container">
    <div class="card shadow-sm mb-5">
        <div class="card-header bg-white fw-bold">إضافة حساب جديد</div>
        <div class="card-body">
            <form action="/add-account" method="POST" enctype="multipart/form-data" class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">عنوان الحساب</label>
                    <input type="text" name="title" class="form-control" placeholder="مثلاً: حساب أساطير ليفربول" required>
                </div>
                <div class="col-md-3">
                    <label class="form-label">السعر ($)</label>
                    <input type="number" name="price" class="form-control" required>
                </div>
                <div class="col-md-3">
                    <label class="form-label">النجوم</label>
                    <input type="number" name="stars" class="form-control" value="5">
                </div>
                <div class="col-md-8">
                    <label class="form-label">اختر صورة الحساب (Screenshot)</label>
                    <input type="file" name="imageFile" class="form-control" accept="image/*" required>
                </div>
                <div class="col-md-4 d-flex align-items-end mb-2">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" name="featured" id="f">
                        <label class="form-check-label" for="f">تمييز الحساب (يظهر في الأعلى)</label>
                    </div>
                </div>
                <div class="col-12">
                    <label class="form-label">أهم اللاعبين (Messi, Cruyff...)</label>
                    <input type="text" name="players" class="form-control" placeholder="افصل بين الأسماء بفاصلة">
                </div>
                <div class="col-12 text-center">
                    <button type="submit" class="btn btn-primary px-5 fw-bold">رفع الحساب ونشره</button>
                </div>
            </form>
        </div>
    </div>

    <div class="table-responsive bg-white p-3 rounded shadow-sm">
        <table class="table table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>الصورة</th>
                    <th>العنوان</th>
                    <th>السعر</th>
                    <th>إجراءات</th>
                </tr>
            </thead>
            <tbody>
                <% accounts.forEach(acc => { %>
                <tr>
                    <td><img src="<%= acc.img %>" width="60" class="rounded"></td>
                    <td><%= acc.title %></td>
                    <td><%= acc.price %>$</td>
                    <td><a href="/delete/<%= acc.id %>" class="btn btn-sm btn-danger">حذف الحساب</a></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</div>
</body>
</html>
