<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>إدارة WassitDZ Game</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container">
            <span class="navbar-brand">لوحة تحكم WassitDZ</span>
            <a href="/logout" class="btn btn-danger btn-sm">خروج</a>
        </div>
    </nav>

    <div class="container">
        <div class="row">
            <div class="col-md-5">
                <div class="card shadow-sm p-4 mb-4">
                    <h5 class="fw-bold mb-3">إضافة حساب جديد</h5>
                    <form action="/add-account" method="POST" enctype="multipart/form-data">
                        <input type="text" name="title" class="form-control mb-2" placeholder="عنوان الحساب" required>
                        <input type="number" name="price" class="form-control mb-2" placeholder="السعر $" required>
                        <textarea name="players" class="form-control mb-2" placeholder="أهم اللاعبين (Messi, Neymar...)"></textarea>
                        
                        <label class="small text-muted mb-1">اختر صور الحساب (حتى 5 صور)</label>
                        <input type="file" name="imageFiles" class="form-control mb-3" accept="image/*" multiple required>
                        
                        <div class="form-check form-switch mb-3">
                            <input class="form-check-input" type="checkbox" name="featured" id="f">
                            <label class="form-check-label" for="f">تمييز الحساب 🔥</label>
                        </div>
                        <button class="btn btn-primary w-100 fw-bold">نشر الحساب</button>
                    </form>
                </div>
            </div>
            <div class="col-md-7">
                <div class="card shadow-sm p-4">
                    <h5 class="fw-bold mb-3">الحسابات الحالية</h5>
                    <table class="table align-middle">
                        <thead><tr><th>الصور</th><th>العنوان</th><th>السعر</th><th>حذف</th></tr></thead>
                        <tbody>
                            <% accounts.forEach(acc => { %>
                            <tr>
                                <td><img src="<%= acc.imgs[0] %>" width="50" class="rounded shadow-sm"></td>
                                <td class="small">#<%= acc.id %> - <%= acc.title %></td>
                                <td class="fw-bold text-success">$<%= acc.price %></td>
                                <td><a href="/delete/<%= acc.id %>" class="btn btn-sm btn-outline-danger">حذف</a></td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
