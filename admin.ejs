<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>الإدارة</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">
    <div class="container shadow-sm bg-white p-4 rounded">
        <div class="d-flex justify-content-between mb-4">
            <h3>إضافة حساب جديد</h3>
            <a href="/logout" class="btn btn-danger btn-sm">خروج</a>
        </div>
        <form action="/add-account" method="POST" enctype="multipart/form-data" class="row g-3">
            <div class="col-md-6"><input type="text" name="title" class="form-control" placeholder="عنوان الحساب" required></div>
            <div class="col-md-3"><input type="number" name="price" class="form-control" placeholder="السعر $" required></div>
            <div class="col-md-3"><div class="form-check mt-2"><input class="form-check-input" type="checkbox" name="featured" id="f"><label for="f">عرض محدود 🔥</label></div></div>
            <div class="col-12"><input type="text" name="players" class="form-control" placeholder="اللاعبين"></div>
            <div class="col-12"><input type="file" name="imageFile" class="form-control" accept="image/*" required></div>
            <div class="col-12"><button class="btn btn-primary w-100">نشر الحساب</button></div>
        </form>
        <hr>
        <table class="table mt-4">
            <thead><tr><th>العنوان</th><th>السعر</th><th>حذف</th></tr></thead>
            <tbody>
                <% accounts.forEach(acc => { %>
                <tr><td><%= acc.title %></td><td><%= acc.price %>$</td><td><a href="/delete/<%= acc.id %>" class="text-danger">حذف</a></td></tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
